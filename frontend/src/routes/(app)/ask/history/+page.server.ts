/**
 * Server-side logic for conversation history page
 *
 * Minimal load function — client handles all conversation fetching
 * since it needs the auth token from Supabase.
 */

export async function load() {
	return {};
}
