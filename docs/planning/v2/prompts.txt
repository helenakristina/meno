I'm removing the fake/hallucinated URLs from scenario suggestions. We'll keep strong evidence-based language but remove citations until we have proper RAG.

## What Changed

Remove all URL generation from scenario suggestions. Keep the evidence-based claims, just don't cite fake sources.

## File: `backend/app/services/llm.py`

In `generate_scenario_suggestions()`, replace the user_prompt:
```python
user_prompt = f"""Generate a confident, evidence-based response for this dismissal scenario.

CONTEXT: Patient's urgent concern is: {goal}

DISMISSAL: "{scenario_title}"

Patient Context:
- Appointment type: {appointment_type}
- Her concerns: {', '.join(concerns) if concerns else 'Not specified'}

Generate a response she can use IN THIS EXACT APPOINTMENT. Requirements:

1. Acknowledge the provider's concern (don't dismiss them)
2. Provide evidence-based reasoning (don't make up sources)
3. Redirect to actionable discussion
4. Keep to 2-3 sentences (realistic for appointment)

CRITICAL: Do NOT include URLs or citations. We don't have verified sources to cite yet.
Focus on clear, confident, evidence-based language.

Examples of strong responses (use these as templates):

DISMISSAL: "Hot flashes will go away on their own"
RESPONSE: "They might eventually, but I've had them for several months and they're significantly impacting my sleep and work. Research shows hormone therapy can be effective for hot flashes. Can we discuss what timeline you're thinking and what options exist in the meantime?"

DISMISSAL: "Brain fog is just normal aging"
RESPONSE: "I understand, but research shows cognitive changes during perimenopause can be significant and distinct from normal aging. Can we talk about whether treatment might help in my case?"

DISMISSAL: "You should see a sleep specialist instead"
RESPONSE: "I'm open to that if needed, but I'd like to explore what you can do first since this is directly related to my perimenopause. Can we start with your approach?"

DISMISSAL: "Let's try an antidepressant first"
RESPONSE: "I appreciate that option, but my anxiety started with my perimenopause symptoms. Can we discuss whether hormone therapy might address both?"

Guidelines:
- Sound confident and informed
- Reference "research shows" where appropriate (without fake citations)
- Be collaborative, not confrontational
- Be specific to the dismissal
- Don't apologize or minimize her concerns
- Don't invent sources

Output as JSON:
{{
  "suggestion": "Full response text (no URLs)",
  "sources": []
}}"""
```

## Update Response Model

File: `backend/app/models/appointment.py`
```python
class ScenarioCard(BaseModel):
    """Single scenario card for appointment prep."""
    id: str
    title: str
    situation: str
    suggestion: str  # Evidence-based, no URLs
    category: str
    sources: list[str] = []  # Empty for now (reserved for RAG)
```

## Update Frontend Display

File: `frontend/src/routes/(app)/appointment-prep/Step4Scenarios.svelte`

Remove the sources section:
```svelte
<div class="space-y-2">
  <p class="text-slate-700">{scenario.suggestion}</p>
  
  <!-- REMOVE sources display -->
  <!-- {#if scenario.sources && scenario.sources.length > 0}
    <div class="pt-2 border-t border-slate-200">
      ...
    </div>
  {/if} -->
</div>
```

Also update the personal cheat sheet PDF generation to NOT include sources in "If Things Go Sideways":

File: `backend/app/services/llm.py`

In `generate_pdf_content()` for personal_cheatsheet, update:
```python
# Build "If Things Go Sideways" section from scenarios
sideways_section = ""
if dismissals:
    sideways_items = []
    for dismissal in dismissals:
        # dismissal format: "If they say: '...' You can say: '...'"
        sideways_items.append(dismissal)
    
    sideways_section = "5. 'IF THINGS GO SIDEWAYS'\n\n" + "\n\n".join(sideways_items)
```

## Add to V2 Roadmap

File: `docs/planning/V2_V3_ROADMAP.md`

Add to V2 or post-launch section:
```
## High Priority: RAG for Scenarios (Post-V2)

Implement proper RAG to ground scenario responses in real sources:
- Retrieve actual NAMS guidelines for each symptom
- Retrieve peer-reviewed research papers
- Generate real, verified links
- Replace "research shows" with actual citations

This will enable:
- Credible, verifiable evidence
- Real URLs user can click
- Trust in the tool
- Professional quality

Estimated effort: 2-3 days
Blocker: Need access to curated knowledge base (NAMS, PubMed, etc.)
```

## That's it

Quick, clean, honest. No fake citations. Strong language without made-up sources.

Ready to run it?